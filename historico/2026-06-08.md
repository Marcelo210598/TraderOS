# Snapshot - 08/06/2026

## 🎯 O que foi feito hoje
- **Auditoria minuciosa pré-soft-launch** do TraderOS (pasta + repo + URL ao vivo) pra decidir se dá pra abrir pra comunidade Nômade Trader.
- **Confirmação das env vars na Vercel** via `vercel env ls production` — TODAS as 9 presentes (o `progress.md` marcava 3 como pendentes; estava desatualizado).
- **Criado `scripts/promote-users.mjs`** — promove alunos pra PRO (ou outro plano) por email, já que não há checkout de pagamento.
- **Testado o script em dry-run** (conecta no Neon, busca usuário, detecta não-cadastrado) — funcionando.
- **TRILHA DE APRENDIZADO CONSTRUÍDA** (era "em breve") — 5 módulos, 30 aulas escritas em texto:
  - Conteúdo: `src/lib/trilha-content.ts` (Fundamentos, Gestão de Risco, Leitura de Mercado, Psicologia, Apex). Fácil de editar.
  - Progresso por aula via localStorage: `src/lib/trilha-progress.ts` (client-side, sem migration).
  - Componentes: `src/components/trilha/` (trilha-overview, module-view, lesson-content).
  - Rotas: `/trilha` (overview + progresso) e `/trilha/[moduleId]` (viewer com prev/next e "marcar concluída").
  - Liberada pra TODOS os planos (sem gate). Build limpo (47/47), deployada via CLI.
  - ⏳ Marcelo vai ler o conteúdo e dar feedback depois.
- Atualizado `progress.md` (env vars + soft-launch + Trilha).

## 📝 Decisões técnicas importantes
- **Soft-launch = grátis pra poucos alunos selecionados** (validação). Sem implementar pagamento por enquanto.
- Acesso PRO concedido **manualmente via script no banco**, não por compra.
- Script usa o mesmo padrão Prisma 7 do projeto (`PrismaPg` adapter + `dotenv`), conectando no `DATABASE_URL` do `.env` (mesmo Neon de produção).

## ⚠️ Problemas encontrados e soluções
- **Análise inicial apontou "gate de Setups sem proteção no backend"** → FALSO. Verifiquei na mão: `src/app/api/setups/route.ts:63` retorna 403 pra plano FREE. Está protegido.
- **`progress.md` dizia que ANTHROPIC/RESEND/UPLOADTHING estavam pendentes** → na verdade já estavam na Vercel. Corrigido.
- **Warning de SSL do `pg`** ao rodar o script → apenas aviso de depreciação, inofensivo.

## 🔧 Configurações adicionadas
- Nenhuma env var nova (todas já existiam na Vercel).
- Novo arquivo: `scripts/promote-users.mjs`.

## 📊 Estado atual do projeto
- **Núcleo 100% funcional e seguro** pra uso real: Journal, Guardian Apex, Analytics, Check-in, Setups, IA Vega/Ask Claude, Sync NinjaTrader, gamificação, auth (Google + credentials).
- **Pronto pra soft-launch grátis.** Falta só receber a lista de emails dos alunos e rodar o script.
- **Bloqueador pra venda real (não pra validação):** pagamento não implementado (botão `/planos` sem `onClick`).

## 🚧 Próxima sessão (To-do)
- [ ] **Receber feedback do Marcelo sobre o conteúdo da Trilha** (ele vai ler e comentar).
- [ ] Receber lista de emails dos alunos testadores → rodar `node scripts/promote-users.mjs <emails>`.
- [ ] (Quando escalar) Rate limiting nas APIs de IA + implementar pagamento (Stripe/Abacatepay).
- [ ] (Opcional) Dar XP ao concluir aula da Trilha (hoje o progresso é só localStorage, sem XP).

## 💡 Observações importantes
- Após promover um aluno, ele precisa fazer **logout/login** — o NextAuth v5 cacheia o plano no JWT por até 1h.
- O aluno precisa **se cadastrar primeiro**; o script avisa quem ainda não tem conta e pula.
