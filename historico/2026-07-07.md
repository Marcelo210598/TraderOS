# Snapshot - 07/07/2026

## 🎯 O que foi feito hoje

### 1. Separação automática de contas (Teste/Avaliação/Aprovada) — commit e75f178
- `detectAccountLabel` reescrito: detecta o TIPO de conta pelo nome que a corretora manda:
  - `Sim`/`Demo`/`Playback` → **TEST** (simulação, não conta nas métricas)
  - `APEX-######` → **EVAL** (avaliação/aprovação, paga)
  - `PAAPEX`/`PA##K` → **PA** (funded, saque real)
- `Trade.accountName` novo: guarda o nome bruto da conta (auditável)
- `TradingAccount.brokerName` novo: identidade real → cada conta da corretora vira uma
  TradingAccount própria. Unique trocado p/ (userId, source, brokerName).
- `ensureAccount(userId, source, label, brokerName)` separa por conta real; se o tipo
  muda (avaliação→funded), atualiza o label sozinho.
- Taxonomia (`src/lib/accounts.ts`) ganhou EVAL (Avaliação) e PA (Aprovada) — **corrigiu o
  bug real do "tudo vira Conta Teste"**: labels desconhecidos (`PA` puro) caíam no fallback
  ACCOUNT_OPTIONS[0]=Teste.
- Dashboard + journal excluem teste + arquivadas por padrão (`excludeTestTrades`).
- Journal ganhou filtro por conta; Carteira mostra badge de tipo.
- **Dados do Marcelo reorganizados**: 18 trades PA→EVAL; conta de avaliação PERDIDA
  (16 trades de jun) separada em "Avaliação (perdida)" arquivada; 2 atuais em "Avaliação (atual)".
  Scripts: `scripts/relabel-eval-account.mjs`, `scripts/split-lost-account.mjs`.

### 2. Fix trade duplicado "espelho" no sync NT8 — commit 0860440
- Bug: numa reconexão o NinjaTrader reprocessa execuções do dia e reconstrói o MESMO
  trade INVERTIDO (entrada↔saída trocadas, direção oposta, mesmo |pnl|) com externalId novo.
  O dedup por externalId não pegava. Caso 06/07: 1 operação virou 2 (dois MNQ +$134.5 espelho).
- Fix: sync rejeita o espelho exato dentro de janela de 12h. Fantasma 06/07 apagado.

### 3. Painel de uso no Admin — commit 6846c4d
- `/admin/uso`: usuários totais, online agora (lastSeen<5min), ativos hoje/7d, cadastros,
  distribuição por plano (Free/Trader/Pro), engajamento (users com trade, trades 7d/30d).
- `User.lastSeenAt` novo (throttle 3min no layout do app), `src/lib/presence.ts`, `src/lib/admin-stats.ts`.
- Hoje: só 2 users (Marcelo + Andersson, ambos PRO) — app ainda não aberto ao público.

### 4. Retargeting Meta completo — commits 6846c4d + adaee72
- Pixel MeuTrade **1705413327409397** criado e no ar (firando no meutrade.app).
- Eventos: PageView + ViewContent (viu planos) + InitiateCheckout (clicou assinar) client-side
  (`src/lib/fbpixel.ts`); Lead (cadastro) + Purchase (webhook Asaas) server-side via
  Conversions API (`src/lib/fbcapi.ts`, dedup por payment.id).
- Env vars na Vercel: `NEXT_PUBLIC_META_PIXEL_ID` + `META_CAPI_TOKEN` (validado, events_received:1).

## 📝 Decisões técnicas importantes
- Separação de conta é AUTOMÁTICA pelo nome da corretora — não depende de tag manual (Marcelo insistiu nisso).
- CAPI (server-side) escolhida p/ Lead/Purchase em vez de client-side — mais confiável (não depende do fbq carregar).
- Migrations idempotentes (IF NOT EXISTS) porque prod é gerida por `db push` (histórico dessincronizado).

## ⚠️ Problemas encontrados e soluções
- **Migrations dessincronizadas** (prod usa db push): `add_notifications` marcada como falha (erro 42710).
  Solução: `prisma migrate resolve --applied 20260524000002_add_notifications` antes do deploy.
- **Meta flagou o business MeuTrade** por ter sido criado via automação (navegador Claude) → restrição.
  Solução: Marcelo clicou "Pedir análise" (manual) e a Meta liberou em minutos.
  **LIÇÃO: criar conta/business na Meta é sempre CLIQUE MANUAL, nunca automação.**
- **Token CAPI falhou 1x** (erro temporário da Meta em dataset novo) → funcionou na 2ª tentativa.

## 🔧 Configurações adicionadas (Vercel, produção)
- `NEXT_PUBLIC_META_PIXEL_ID` = 1705413327409397
- `META_CAPI_TOKEN` = (token da Conversions API, encriptado)

## 📊 Estado atual do projeto
- Separação de contas: ✅ no ar e validada
- Fix duplicado: ✅ no ar
- Painel de uso: ✅ no ar (/admin/uso)
- Retargeting Meta: ✅ 100% no ar (pixel + 5 eventos + CAPI)

## 🚧 Próxima sessão (To-do)
1. **Meta — conta de anúncios**: business MeuTrade não tem conta de anúncios. Criar UMA NOVA
   dentro do MeuTrade (Configurações da empresa → Contas de anúncios → Adicionar → Criar) +
   vincular cartão. **MANUAL** (não automatizar — flag risk). Aí o pixel já é visível pra ela.
2. **Meta — públicos de retargeting**: esperar ~3-5 dias o pixel juntar ≥100 pessoas, depois
   criar público personalizado (fonte Pixel MeuTrade). Ex "InitiateCheckout sem Purchase".
3. **Google/GA4**: setar `NEXT_PUBLIC_GA_ID` (GA4) e/ou `NEXT_PUBLIC_GOOGLE_ADS_ID` na Vercel.
4. **Push no iPhone do Marcelo**: instalar PWA (Compartilhar→Adicionar à Tela de Início),
   abrir pelo ícone, Configurações→Ativar notificações→Enviar teste. (Zero subscriptions hoje = por isso não chega push.)
5. **Override de tipo de conta no app** (deferido): deixar o user mudar Teste/Avaliação/Aprovada na conta.
6. Reclassificar (opcional) os 5 trades "Manual · PA" antigos do Marcelo se estiverem errados.
7. Confirmar email de verificação do portfólio MeuTrade (chegou no Gmail do Marcelo).

## 💡 Observações importantes
- Vercel Analytics é ANÔNIMO (LGPD) — não dá nome de visitante. Retargeting por pixel é o caminho.
- Tráfego do Marcelo vem de IG/FB (referrers FB 12 / IG 6+6 no Vercel Analytics 07/07).
- Conta de anúncios existente (898100818198190) está no business Black White Shop — Marcelo NÃO quer usar essa.
- Businesses do Marcelo: BM-Black White Shop, CaliForce, e o novo MeuTrade (1533400541871070).
