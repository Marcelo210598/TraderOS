# Snapshot — 19/05/2026

## 🎯 O que foi feito hoje
- Banco Neon PostgreSQL conectado e migration inicial aplicada (13 tabelas)
- Google OAuth configurado (Client ID e Secret no .env)
- AUTH_SECRET gerado com openssl
- **Módulo Journal completo:**
  - API GET/POST /api/trades (com paginação, filtros, limite plano Free)
  - API GET/PATCH/DELETE /api/trades/[id]
  - Listagem com filtros (resultado, ativo, setup, data de/até)
  - Formulário novo trade (PnL calculado automaticamente por ativo)
  - Detalhe do trade com todos os dados + slots de screenshot + análise IA (Pro)
  - Edição de trade
  - Deleção com confirmação
  - Paginação com navegação por páginas
  - Stats mensais (total, win rate, P&L) + alerta plano Free ≥ 8/10 trades
  - XP +10 por trade registrado
- **Check-in Emocional:**
  - API GET/POST /api/checkin
  - Formulário com 5 métricas (emocional, energia, foco, confiança, ausência de stress)
  - Scale visual 1-10 com feedback de cor (verde/amarelo/vermelho)
  - Avaliação geral + recomendação (pronto / cautela / não operar)
  - Registro pré e pós-sessão por dia
  - XP +5 por check-in
- **Biblioteca de Setups (plano Trader+):**
  - API GET/POST /api/setups e PATCH/DELETE /api/setups/[id]
  - Cards com stats por setup (total trades, win rate, P&L total)
  - Modal de criação/edição
  - Soft delete (arquivamento)
  - Gate de plano Free com CTA de upgrade

## 📝 Decisões técnicas
- PnL calculado automaticamente no formulário com base no par de preços + point value por ativo
- Limite Free verificado no server-side (POST /api/trades) para segurança
- Setups: stats calculados em tempo real a partir dos trades (não armazenados)
- Check-in: PRE antes de POST na mesma sessão

## 📊 Estado atual do projeto
- ~55% completo
- Auth: ✅ funcional (Google OAuth + credentials configurados)
- Banco: ✅ Neon conectado, migration aplicada
- Journal: ✅ CRUD completo, filtros, paginação
- Check-in: ✅ completo
- Setups: ✅ CRUD completo com gate de plano
- Progress: 🔴 placeholder
- Guardian: 🔴 placeholder
- Uploads de screenshot: 🟡 estrutura pronta, UploadThing não configurado

## 🚧 Próxima sessão
1. Módulo Progress (XP real, level up, achievements, streaks)
2. Módulo Guardian (trailing drawdown, consistency rule, scaling Apex)
3. Integrar UploadThing para screenshots no Journal
4. Página de cadastro (/cadastro) com email de boas-vindas via Resend
5. Página de planos (/planos) com comparativo e CTA de upgrade
