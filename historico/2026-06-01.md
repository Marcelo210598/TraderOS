# Snapshot — 01/06/2026

## 🎯 O que foi feito hoje

### Fix crítico: Vercel Analytics e Speed Insights funcionando em produção
- Investigado o `npm error Invalid Version: ` que causava falha no `npm install` no Vercel
- Identificado o pacote culpado: `@unrs/resolver-binding-openharmony-arm64` (dep opcional de `unrs-resolver` → `eslint-import-resolver-typescript`)
- A entrada no `package-lock.json` estava sem o campo `version` (package para OpenHarmony ARM64 — SO chinês — que nunca é instalado localmente, mas fica no lockfile sem versão)
- Fix: 1 linha adicionada no lockfile — `"version": "1.12.0"` na entrada problemática
- Commit e push — deploy Vercel disparado

## 📝 Decisões técnicas importantes
- O `@vercel/analytics` e `@vercel/speed-insights` JÁ estavam corretamente importados no `layout.tsx` desde o commit anterior. O problema nunca foi o código — era só o lockfile corrompido
- `npm error Invalid Version: ` (string vazia) = campo `version` ausente num pacote do lockfile
- Solução direta no lockfile é melhor que `--omit=optional` global (quebraria os `@img/sharp` que o Next.js usa na Vercel)

## ⚠️ Problemas encontrados e soluções
- **Problema:** Build Vercel falhava com `npm error Invalid Version: ` desde o commit "feat: adiciona Vercel Analytics e Speed Insights"
- **Causa:** `package-lock.json` tinha entrada sem `version` para `@unrs/resolver-binding-openharmony-arm64`
- **Solução:** Adicionar `"version": "1.12.0"` na entrada do lockfile

## 🔧 Configurações adicionadas
- `@vercel/analytics@2.0.1` → rastreia visitantes
- `@vercel/speed-insights@2.0.0` → rastreia performance
- Ambos no `layout.tsx` como `<Analytics />` e `<SpeedInsights />`

## 📊 Estado atual do projeto
- ~90% do MVP concluído
- Vercel Analytics e Speed Insights ativos em produção
- Próximos grandes itens: domínio traderos.app + Stripe

## 🚧 Próxima sessão (To-do)
1. Domínio traderos.app (~15 min de DNS)
2. Stripe — gateway de pagamento (R$97 Trader / R$197 Pro)
3. NinjaTrader — reescrever NinjaScript como Strategy (aguarda teste Sim101)

## 💡 Observações importantes
- Build local funciona mesmo com lockfile corrompido (npm local ignora versão vazia em opcionais não instalados)
- Vercel usa versão diferente do npm que valida mais estritamente
- Commit do fix: `27174fa`
