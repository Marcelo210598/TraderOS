# Snapshot - 02/07/2026

## 🎯 O que foi feito hoje
- Sessão de **diagnóstico do sync MT5** (não teve alteração de código, só investigação no banco).
- Contexto: **Matheus iniciou hoje o copytrade** (a conta dele espelha a do Marcelo). Objetivo: garantir que quando a operação fechar, o trade "acuse" (apareça + dispare alerta) no TraderOS.
- Auditoria do banco Neon (scripts de leitura temporários, já removidos):
  - **Usuários no TraderOS:** só 2 — Marcelo e Andersson (ambos PRO). **Matheus NÃO tem conta.**
  - **API keys:** 3 do Marcelo (2x "NinjaTrader", 1x "MT5-Windows-BMS") + 1 do Andersson ("NinjaTrader").
  - **Sync MT5 NUNCA recebeu trade:** key "MT5-Windows-BMS" com `lastUsed: —`, **zero trades** `source=MT5`.
  - **Único sync vivo:** NT8 do Marcelo — postou hoje 16:29 (MNQ SHORT +65.5). Todos os trades históricos são NinjaTrader, label **TEST** (conta Sim101).
- **Fim do dia: Matheus não teve entrada** (por isso nada chegou — esperado).

## 📝 Decisões / confirmações técnicas
- **Alerta de trade do MT5 CONFIRMADO no código:** `src/app/api/sync/mt5/route.ts` (linhas 139-153) chama `createTradeAlert(userId, ...)` igual o NT8 → notificação in-app (sino/toast) + Web Push. Dispara pro `userId` **dono da API key** do sync. Ou seja: qualquer user que conectar o MT5 com a própria key recebe alerta automático a cada trade fechado.
- **EA `metatrader/TraderOSSync.mq5`** posta em `/api/sync/mt5` quando a posição FECHA (`OnTradeTransaction`). Config exigida: ApiKey (≥10 chars), WebRequest liberado p/ `https://trader-os-ashy.vercel.app`, AlgoTrading ligado.

## ⚠️ Problemas / riscos encontrados
- **Fluxo MT5 nunca foi validado em produção com TRADE** (só saldo/carteira em 20/06). Não há garantia de que um trade real vai chegar até o EA ser testado ao vivo com uma posição fechando.
- **Gotcha do "espelho":** o EA posta com a key que está NELE. Se rodar na conta do Matheus com a key do MARCELO → o trade cai na conta do Marcelo e o alerta vai pro Marcelo, não pro Matheus.
- **Trades do Marcelo estão todos como TEST (Sim101).** Se o copytrade agora é conta REAL, precisa ajustar pra registrar como REAL/MT5 (senão não conta nas métricas reais). — decisão pendente.

## 📊 Estado atual
- Sync NT8: ✅ em produção, validado, alertas OK.
- Sync MT5: código pronto + alertas plugados, mas **0 trades em prod** — aguardando primeira entrada real do copytrade do Matheus pra validar de ponta a ponta.

## 🚧 Próxima sessão (To-do)
- Quando o Matheus abrir a próxima operação: confirmar na aba **Experts** do MT5 dele que aparece `[TraderOS] EA conectado` (sem `⚠️ API Key` / `❌ URL não autorizada`).
- Monitorar o banco no fechamento da 1ª operação pra validar que o trade MT5 chega + alerta dispara.
- Decidir: (a) Matheus terá conta/login próprio no TraderOS? (b) trades devem virar REAL em vez de TEST?

## 💡 Observações
- Endpoints MT5: `/api/sync/mt5` (trade), `/api/sync/mt5/balance` (saldo), `/api/sync/mt5/transaction` (depósito/saque).
- Ver memória [[project_traderos_mt5]] e [[project_traderos_carteira]].
