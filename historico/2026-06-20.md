# Snapshot - 20/06/2026 (sessão longa)

## 🎯 O que foi feito

### Alertas de trade
- **Toast in-app** em tempo real quando chega trade via sync (sino com polling 30s, verde WIN/vermelho LOSS).
- **Web Push** (igual Instagram/Nômade): `web-push` + VAPID (3 env vars na Vercel), tabela `PushSubscription`, service worker v2 com handlers push/notificationclick, rotas `/api/push/subscribe` e `/api/push/test`, componente em Configurações com guia iOS (PWA). Disparo no `createTradeAlert`.

### Fix crítico (Base UI)
- Menu do avatar quebrava ao abrir em QUALQUER página (**Base UI error #31** = `MenuGroupRootContext is missing`): `DropdownMenuLabel` (GroupLabel) estava fora de `DropdownMenuGroup`. Corrigido + item vira link via `render` prop. Também trocado "Meu Perfil" (404 → /perfil inexistente) por "Configurações".

### Reposicionamento — "app do trader" (não mais da Apex)
- **Guardian aposentado**: fora da sidebar/bottom-nav (→ Calendário), acessível discreto em Configurações. Não deletado.
- Identidade genérica (futuros/forex/ações) em manifest, landing, onboarding.

### Integração MetaTrader 5 (do zero)
- EA `TraderOSSync.mq5` (MQL5), endpoint `/api/sync/mt5`, `Trade.quantity` → Decimal (lotes), plataforma MT5 + tutorial de 8 passos com 13 prints reais.
- **Conectado na conta REAL do BMS (Broker Mercosul, login 11062)** numa VM Windows (MT5 no Mac/Wine trava ao adicionar broker; mobile não roda EA).

### Carteira (nova página /carteira) — Fase 1 + 2
- Entidade `TradingAccount` + `Trade.source`/`accountId` + `BalanceTransaction`. Backfill das contas.
- Saldo consolidado, **equity curve** (consolidada/por conta), cards por conta, donut de distribuição, histórico/extrato, editar + **arquivar** conta.
- **EA v2.00**: envia saldo/equity real (OnInit + timer 10min + após trade) e captura depósitos/saques. Endpoints `/api/sync/mt5/balance` e `/transaction`. **Validado: saldo real 110 USD do BMS chegou.**

### Login Google — CONSERTADO
- Quebrou com `disabled_client` → projeto Google Cloud antigo **suspenso por credencial vazada**.
- Criado **projeto GCP novo** ("TraderOS") + OAuth client novo (Client ID `74042321917-…`) + consent screen publicada + credenciais atualizadas na Vercel + redeploy. **Validado: Marcelo logou via Google no celular.**

## ⚠️ Pendências / próximos passos
- 🔒 **Segurança (importante):** credencial vazou em repo PÚBLICO (11 repos). Varredura no histórico git pra achar/remover a chave — Marcelo topou fazer ao retomar.
- **Copytrade do Matheus** (~semana 23/06): testar trades reais caindo. VM Windows precisa ficar ligada 24/7.
- Tela de **Integrações orientada a dados** (registry + modal) pra escalar mesas.
- **Stripe + domínio** traderos.app — Marcelo falou julho/2026.
- Melhoria opcional do EA: recuperar 2-3 dias ao reiniciar (cobre fim de semana).

## 💡 Observações
- Conta BMS: senha do MT5 ≠ senha do site; reseta no painel BMS (aba "Conta de Transações").
- Google: secret não é mais exibido após criar (gerar nova + copiar via pbpaste). Redirect NextAuth v5: `{AUTH_URL}/api/auth/callback/google`.
- Tudo commitado e deployado (`trader-os-ashy.vercel.app`).
