# Snapshot - 25/06/2026

## 🎯 O que foi feito hoje

### 1. Correções de layout mobile (overflow)
- Dashboard: botão "Fazer check-in" vazava do card → empilha no mobile (`flex-col sm:flex-row`)
- Journal: badge de multiplicador (1x/5x) cortava na borda → `flex-wrap` na linha de metadados
- Vega (ask-claude): placeholder do input cortava na 2ª linha → encurtado + `whitespace-nowrap`/`truncate` + `min-h` maior
- Commits `9e76fdb`, `6ce226f`

### 2. Definição de monetização (planos + gateway)
- **Gateway escolhido: Asaas** (NÃO Stripe/Mercado Pago — MP já comprometido com afiliado do Rei do Cupom; Asaas isola finanças). Pix taxa baixa + cai rápido.
- **3 planos em escada:** Free R$0 / Starter R$19,90 (25 trades, Vega 5/mês, Guardian, 5 setups, 1 conta, 1 integração) / Pro R$97 ou R$897/ano (tudo ilimitado, Vega 40/mês).
- Modal de upgrade ao bater teto (cada limite = porta de venda). Comunidade Nômade = grátis manual.
- **Custos analisados:** Vega (Haiku 4.5 $1/$5 por 1M tok) ~R$3,50/user com teto. Margem Pro ~93%, Starter ~86%. Fixos: Vercel Hobby grátis hoje (vira Pro ~R$110 ao faturar), Neon/Resend free tier, UploadThing ~R$55 passando 2GB. `FINANCE_FLOW_API_KEY` no .env é var órfã (não usada).

### 3. Painel Admin de controle de acesso (CONSTRUÍDO + EM PROD)
- Página `/admin` (gated por `role === "ADMIN"`, redirect se não for)
- API `/api/admin/users` (GET busca + PATCH muda plano), dupla checagem ADMIN no servidor
- Busca por email/nome + botões Liberar Pro / Bloquear; trava de auto-rebaixamento
- Infos por usuário: cadastro, nº de trades, última atividade (último trade), método de login (Google/Email)
- Layout mobile-first (infos em grid que quebra, botões cheios tap-friendly)
- Helper `src/lib/admin.ts` (`fetchAdminUsers`) compartilhado entre página e API (DRY)
- Link "Admin" na sidebar (só p/ ADMIN)
- Script `scripts/make-admin.mjs` — concede role ADMIN por email. **Marcelo já é ADMIN** (rodado hoje).
- Commits `04ab8b7`, `875e642`

### 4. Push pro admin em novo cadastro
- `notifyAdminsNewSignup()` em `lib/admin.ts` manda Web Push pra todos os admins (reusa `lib/push`)
- Plugado nos 2 fluxos: evento `createUser` do NextAuth (Google) + rota `/api/auth/register` (email/senha)
- Não-bloqueante; push leva pro `/admin` ao tocar, com nome+email do novo user
- Commit `bc1ccc8` (deploy final do dia)

## ⚠️ Problemas encontrados e soluções
- **Erro `disabled_client` no login Google LOCAL:** o `.env` da máquina ainda tinha o Client ID ANTIGO/desativado (`321013464934-...`); só a Vercel fora atualizada em 20/06. Solução: `vercel env pull --environment=production` → troquei `AUTH_GOOGLE_ID`/`AUTH_GOOGLE_SECRET` no `.env` (backup em `.env.bak`). ⚠️ Pendência p/ Google funcionar em localhost: adicionar no GCP origem `http://localhost:3000` + redirect `http://localhost:3000/api/auth/callback/google` (senão dá `redirect_uri_mismatch`). Alternativa local: login email/senha.

## 🔧 Configurações
- `.env` local atualizado com credenciais Google novas (client `74042321917-...`). Backup `.env.bak`.
- Nenhuma env var nova necessária (push/VAPID e Resend já existiam).

## 📊 Estado atual
- MVP ~92%. Controle de acesso manual COMPLETO e em produção.
- Falta só a camada de pagamento (Asaas) pra monetizar.

## 🚧 Próxima sessão (To-do) — COMEÇAR DIRETO NO ASAAS
1. Marcelo abre conta Asaas → ativa Sandbox → pega API Key de **sandbox**
2. Montar: checkout dos 3 planos (Free/Starter/Pro) + webhook que seta plano ao pagar
3. Paywall: bloquear features por plano + modal de upgrade ao bater teto
4. Distinguir acesso manual (comunidade) de pago no webhook (não rebaixar quem foi liberado manual) — avaliar campo `accessSource` no schema
5. Renomear enum `TRADER` → `STARTER` no schema (migration à parte) p/ casar com os planos novos
6. Ao ligar pagamento: migrar Vercel Hobby → Pro

## 💡 Observações
- Enum `Plan` ainda é FREE/TRADER/PRO (TRADER = Starter na UI). Liberar comunidade = setar PRO.
- Push só chega se o admin tiver PWA instalado + notificação permitida (mesma infra dos alertas de trade).
- `make-admin.mjs` e `promote-users.mjs` rodam contra o Neon de produção (DATABASE_URL do .env).
