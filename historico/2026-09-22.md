# Snapshot - 2026-09-22

Sessão curta de checklist/manutenção de documentação — sem mudança de código. Objetivo: revisar pendências do progress.md, confirmar o que já foi resolvido e atualizar a fonte da verdade.

## 🎯 O que foi feito hoje

### 1. Auditoria da documentação vs. estado real do projeto
- `progress.md` e `historico/` estavam parados em 17/09, mas o `git log` mostrou 20 commits entre a noite de 17/09 e 18/09 15:58 nunca documentados: tours guiados contextuais em todas as páginas principais, import de CSV do NinjaTrader em português com detecção multi-conta, seleção múltipla + delete em lote no Journal, correções de conta teste vazando em Analytics/Calendário, fix de filtro de data do Journal, fix de import de CSV não atualizando XP/streaks, fix de onboarding/tour repetindo, OG image dinâmica no compartilhamento de trade, lembrete de streak de check-in em risco.
- Registrado tudo isso no `progress.md` pra não ficar um buraco no histórico.

### 2. Verificação de rate limiting na IA
- Checado `src/lib/rate-limit.ts`: implementação real, Upstash Redis distribuído (fixed window via `INCR`+`PEXPIRE NX`+`PTTL`) com fallback in-memory se o Redis falhar ou não estiver configurado — nunca derruba a request do usuário por causa do limiter.
- Confirmado uso via `enforce()`/`hit()` nos pontos certos: login (8/5min), registro (8/10min), Vega chat `ask-claude` (20/min), análise de trade por IA (15/min), check-in IA (10/min), resumo semanal (5/min). O cron de resumo semanal em si é protegido por `CRON_SECRET`, não precisa de rate limit próprio.
- **Veredito: item fechado, já estava implementado corretamente.**

### 3. Verificação do Semgrep
- `.semgrep-scan/` tem resultado de uma varredura completa em 16/09 (rulesets: `p/security-audit`+`p/secrets` baseline, `p/typescript`+`p/react`+`p/nodejs`+`p/nextjs`, + Trail of Bits).
- Ressalva: essa varredura é **anterior** ao código novo de 17-18/09 (tours, import CSV, fixes de conta teste) — esse código ainda não foi escaneado.
- **Veredito: já rodou uma vez, mas vale rodar de novo quando der (ficou como próximo passo, não bloqueante).**

### 4. Confirmações do Marcelo sobre pendências antigas
- Vídeo de divulgação HeyGen: gravado ✅
- CNPJ / subconta Asaas separada por app: resolvido ✅
- Domínio `traderos.app`: obsoleto (domínio oficial já é `meutrade.app`) ✅
- Sync automático NT8: **não é pra reativar agora** — decisão consciente de continuar pausado (`NT8_ENABLED = false` em `src/lib/integration-flags.ts`, de propósito), "vai demorar um pouco pra voltar com essa feature". Não é uma pendência aberta, é uma decisão já tomada.

## 📝 Decisões técnicas importantes
- Nenhuma mudança de código nesta sessão — só documentação alinhada com a realidade do repo.

## ⚠️ Problemas encontrados e soluções
- Doc desatualizado (progress.md/historico parados em 17/09 enquanto o código seguiu até 18/09) → causa raiz: snapshot de fim de sessão não foi feito no dia 17/18. Corrigido agora; reforçar o hábito de salvar snapshot ao fechar cada sessão, mesmo em sessões maratona.

## 🔧 Configurações adicionadas
- Nenhuma.

## 📊 Estado atual do projeto
- meutrade.app no ar, sem pendências de segurança abertas (rate limit ok, Semgrep ok até 16/09).
- Pendências reais restantes: rodar Semgrep de novo (código pós-16/09), Stripe (baixa prioridade, Asaas já cobre), NinjaTrader AddOn de sync (aguardando teste de compilação, mas sem pressa — sync fica pausado por decisão), confirmar disparo do email de boas-vindas, Pix no checkout Asaas, nome fantasia/logo no Asaas.

## 🚧 Próxima sessão (to-do)
- Rodar Semgrep de novo cobrindo o código de 17-18/09.
- Quando decidir voltar com sync automático: testar compilação do AddOn NinjaTrader antes de religar a flag.

## 💡 Observações importantes
- Sessão só de auditoria/doc — nenhum commit de código, apenas `progress.md` + este snapshot.
